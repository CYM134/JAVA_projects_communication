package app_frame;
import appServer.service.appServer;
/**
 * 该类创建APP的Server，作为服务器启后台的服务，运行服务端的类
 */
public class appFrame {
    public static void main(String[] args) {

        new appServer();
    }
}
